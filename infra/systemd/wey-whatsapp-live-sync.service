[Unit]
Description=Captura continua do WhatsApp no cache privado do Wey
After=network-online.target openclaw-gateway.service
Wants=network-online.target
StartLimitIntervalSec=10min
StartLimitBurst=10

[Service]
Type=simple
UMask=0077
ExecStart=/usr/local/bin/wacli --store /root/.local/state/wacli-confinex sync --follow --presence-mode quiet --max-db-size 2GB --max-reconnect 0 --stale-threshold 60s
# O heartbeat decide quando reiniciar. Saida por sessao revogada nao pode gerar
# loop; uma sessao expirada exige novo pareamento, nao repeticao do processo.
Restart=no
TimeoutStopSec=30s

[Install]
WantedBy=multi-user.target
